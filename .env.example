# Copy to .env and fill in locally (never commit the real .env)
PYPI_TOKEN= pypi-xxxxx           # optional if you use token-based publish locally
TEST_PYPI_TOKEN= pypi-xxxxx      # optional for TestPyPI test releases
